# IO #
def FILE {}

def i32 putchar(i8 c)
def i32 printf([i8] format, ?)
def [FILE] fopen([i8] filename, [i8] modes)
def i32 fclose([FILE] stream)
def i32 fread([void] ptr, i64 size, i64 count, [FILE] stream)
def i32 fwrite([void] ptr, i64 size, i64 count, [FILE] stream)
def i32 fprintf([FILE] stream, [i8] format, ?)

# MATH #
def f64 sin(f64 x)
def f64 cos(f64 x)
def f64 floor(f64 x)
def f64 sqrt(f64 x)
def f64 abs(f64 x)

# OTHER #
def f64 random()
def f64 millis()
