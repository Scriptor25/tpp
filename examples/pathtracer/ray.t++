include "vec3.t++"

:ray
def new() -> object = {
    origin = vec3:new(),
    direction = vec3:new()
}

def new(origin, direction) -> object = {
    origin = origin,
    direction = direction
}

def at(self, t) -> object = vec3:add(self.origin, vec3:nmul(t, self.direction))
:ray
